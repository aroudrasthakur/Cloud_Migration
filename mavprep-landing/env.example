# ===========================================
# MavPrep Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to version control!
# ===========================================

# ============ AWS Cognito (Client-side) ============
# These are exposed to the browser (NEXT_PUBLIC_ prefix)
# Get these from AWS Cognito Console > User Pool > App Integration

NEXT_PUBLIC_COGNITO_USER_POOL_ID=us-east-2_XXXXXXXXX
NEXT_PUBLIC_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
NEXT_PUBLIC_AWS_REGION=us-east-2

# ============ AWS DynamoDB (Server-side) ============
# These are only available on the server (API routes)
# Create an IAM user with DynamoDB access and get credentials

AWS_REGION=us-east-2
AWS_ACCESS_KEY_ID=AKIAXXXXXXXXXXXXXXXX
AWS_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DYNAMODB_TABLE_NAME=MavPrepData

# ============ Application (Production) ============
# Set this to your production URL when deploying

NEXT_AUTH_URL="http://localhost:3000"

# ===========================================
# Setup Instructions:
# ===========================================
# 
# 1. AWS Cognito Setup:
#    - Go to AWS Cognito Console
#    - Create a User Pool (or use existing)
#    - Create an App Client (public, no secret)
#    - Copy User Pool ID and Client ID
#
# 2. AWS DynamoDB Setup:
#    - Create a table named "MavPrepData"
#    - Partition Key: PK (String)
#    - Sort Key: SK (String)
#    - Create IAM user with DynamoDB permissions
#    - Generate access keys for the IAM user
#
# 3. IAM Policy (minimum permissions):
#    {
#      "Version": "2012-10-17",
#      "Statement": [{
#        "Effect": "Allow",
#        "Action": [
#          "dynamodb:GetItem",
#          "dynamodb:PutItem",
#          "dynamodb:UpdateItem",
#          "dynamodb:DeleteItem",
#          "dynamodb:Query",
#          "dynamodb:Scan"
#        ],
#        "Resource": "arn:aws:dynamodb:*:*:table/MavPrepData*"
#      }]
#    }
#
# ===========================================

